export default function Topbar() {
  return (
    <div
      style={{
        height: 70,
        display: "flex",
        alignItems: "center",
        justifyContent: "flex-end",
        marginBottom: 24,
      }}
    >
      {/* empty topbar to keep layout spacing */}
    </div>
  );
}